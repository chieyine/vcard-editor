import { InfoPage } from "../../components/site-chrome";

export const metadata = { title: "Cookies — vCard Editor", description: "Cookie and local-storage information for vCard Editor.", alternates: { canonical: "/cookies" } };

export default function CookiesPage() {
  return <InfoPage eyebrow="COOKIES" title="A small browser footprint." intro="The core editor does not require an account or a cookie to open and export a VCF file."><h2>What is used today</h2><p>The launch configuration keeps analytics and advertising disabled by default. The processing workspace uses in-memory browser state and does not require a login cookie.</p><h2>Possible future storage</h2><p>Preferences, mapping presets, consent choices, or optional recovery drafts may use browser storage only when the feature is clearly explained, namespaced by version, time-limited where appropriate, and removable through a visible clear-data action.</p><h2>Optional services</h2><p>If analytics, advertising, consent management, error monitoring, or other third-party services are introduced, this page and the Privacy Policy will identify them, explain their purpose, and describe the available choices. The private processing surface should remain isolated from unnecessary third-party scripts.</p><h2>How to clear local state</h2><p>Use the browser's site-data controls to remove local storage, IndexedDB, service-worker caches, and downloads. Reset the editor before closing a shared-device session.</p></InfoPage>;
}
