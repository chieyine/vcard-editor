# Third-party notices

The application currently uses the following runtime dependencies:

- Next.js, React, and React DOM — MIT licensed;
- SheetJS `xlsx` — Apache-2.0 licensed;
- `fflate` — MIT licensed.

Dependency versions are pinned in `package-lock.json`. Review advisories and upstream licence changes before production releases. Spreadsheet parsing is configured not to evaluate formulas or render imported cell HTML.
